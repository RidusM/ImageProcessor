services:
  integration-test:
    build:
      context: ../../
      dockerfile: tests/integration/Dockerfile
    container_name: integration-test
    depends_on:
      db:
        condition: service_healthy
      db-migrator:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - integration-test-network
    environment:
      CONFIG_PATH: /app/configs/integration-test.env
      INTEGRATION_TEST: "true"
    volumes:
      - ../../configs:/app/configs
    restart: "no"

  db:
    image: postgres:17-alpine
    container_name: notify-db
    environment:
      POSTGRES_DB: ${DB_NAME:-notify_db}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    ports:
      - "${DB_PORT:-5432}:${DB_PORT:-5432}"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - integration-test-network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-notify_db}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: notifier-redis
    command:
      [
        "redis-server",
        "--maxmemory",
        "100mb",
        "--maxmemory-policy",
        "allkeys-lru",
      ]
    ports:
      - "6379:6379"
    networks:
      - integration-test-network

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: notifier-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: rabbitmq-diagnostics check_port_connectivity
      interval: 30s
      timeout: 30s
      retries: 10
    networks:
      - integration-test-network

  db-migrator:
    image: migrate/migrate
    container_name: notifier-db-migrator
    depends_on:
      db:
        condition: service_healthy
    networks:
      - integration-test-network
    volumes:
      - ../../migrations:/migrations:ro
    command:
      - "-path"
      - "/migrations"
      - "-database"
      - "postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=${DB_SSL_MODE}"
      - "up"
    restart: "no"

networks:
  integration-test-network:
    driver: bridge

volumes:
  pgdata:
